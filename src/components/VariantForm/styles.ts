// components/VariantForm/styles.ts
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  card: {
    backgroundColor: '#f2f2f2',
    padding: 12,
    marginTop: 8,
    borderRadius: 6,
  },
  title: {
    fontSize: 16,
    marginBottom: 8,
    fontWeight: '600',
  },
  input: {
    marginBottom: 8,
  },
  switchContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 8,
    justifyContent: 'space-between',
  },
  addButton: {
    marginTop: 4,
  },
});
